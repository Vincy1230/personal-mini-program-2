<!--pages/order/order.wxml-->
<import src="/template/category/category.wxml" />
<view class="submit" bindtap="submitTap">下单</view>
<template is="category" wx:for="{{category}}" data="{{...item, categoryIindex: index}}" wx:key="*this"></template>
<view class="footer">底线, 出现了</view>
